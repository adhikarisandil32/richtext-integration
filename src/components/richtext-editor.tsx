import React from "react"
import { EditorContent, Editor } from "@tiptap/react"
import StarterKit from "@tiptap/starter-kit"

export default function RichtextEditor({ editor }: any) {
  return <EditorContent editor={editor} />
}
